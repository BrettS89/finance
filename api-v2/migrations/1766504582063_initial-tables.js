/**
 * @type {import('node-pg-migrate').ColumnDefinitions | undefined}
 */
exports.shorthands = undefined;

/**
 * @param pgm {import('node-pg-migrate').MigrationBuilder}
 * @param run {() => void | undefined}
 * @returns {Promise<void> | void}
 */
exports.up = (pgm) => {
  pgm.sql(`
    CREATE TYPE expense_frequency AS ENUM (
      'week',
      'month',
      'year'
    );

    CREATE TABLE expense_types (
      id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
      name TEXT NOT NULL,
      frequency expense_frequency NOT NULL,
      budget INTEGER NOT NULL CHECK (budget >= 0),
      description TEXT,
      created_at TIMESTAMPTZ DEFAULT now()
    );

    CREATE TABLE expenses (
      id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
      name TEXT NOT NULL,
      amount NUMERIC(12, 2) NOT NULL CHECK (amount >= 0),
      expenseTypeId BIGINT NOT NULL REFERENCES expense_types(id),
      created_at TIMESTAMPTZ DEFAULT now()
    );

    CREATE INDEX idx_expenses_expense_type_id ON expenses (expenseTypeId);
  `);
};

/**
 * @param pgm {import('node-pg-migrate').MigrationBuilder}
 * @param run {() => void | undefined}
 * @returns {Promise<void> | void}
 */
exports.down = (pgm) => {
  pgm.sql(`
    DROP TABLE expenses;
    DROP TABLE expense_types;

    DROP TYPE expense_frequency;
  `);
};
